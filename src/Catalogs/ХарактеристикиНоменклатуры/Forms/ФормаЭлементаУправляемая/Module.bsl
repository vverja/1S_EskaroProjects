// Процедура перезаполняет таблицу свойств и значений.
//
// Параметры:
//  Характеристика - ссылка на справочник Характеристик, значения свойства которого надо прочитать.
//
&НаСервере
Процедура ПрочитатьЗаполнитьСвойстваИЗначения(Характеристика, Владелец)

	ОбработкаОбъект = РеквизитФормыВЗначение("ОбработкаОбъектЗначенияСвойств");
	ОбработкаОбъект.ОбъектОтбораЗначений   = Характеристика;
	ОбработкаОбъект.ОбъектОтбораНазначений = Владелец;
	ОбработкаОбъект.ПрочитатьЗаполнитьСвойстваИЗначения();

	ЗначениеВРеквизитФормы(ОбработкаОбъект, "ОбработкаОбъектЗначенияСвойств");
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОбработкаОбъектЗначенияСвойств.НазначениеСвойств = ПланыВидовХарактеристик.НазначенияСвойствКатегорийОбъектов.Справочник_ХарактеристикиНоменклатуры;
	
	ПрочитатьЗаполнитьСвойстваИЗначения(Объект.Ссылка, Объект.Владелец);
	
	ТолькоПросмотр = Истина;
	
КонецПроцедуры
