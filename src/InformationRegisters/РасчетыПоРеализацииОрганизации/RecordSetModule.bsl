
Процедура ПередЗаписью(Отказ, Замещение)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Если ЭтотОбъект.Итог("НомерСтрокиДокумента")<ЭтотОбъект.Количество() тогда
		НомерСтроки=0;
		Для каждого СтрокаЗаписи из ЭтотОбъект цикл
			НомерСтроки=НомерСтроки+1;
			СтрокаЗаписи.НомерСтрокиДокумента = НомерСтроки;
		КонецЦикла;
	КонецЕсли;
	ЭтотОбъект.УстановитьАктивность(Истина);
КонецПроцедуры

